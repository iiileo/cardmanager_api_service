services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cardmanager
      POSTGRES_PASSWORD: cardmanager
      POSTGRES_DB: cardmanager
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cardmanager -d cardmanager"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
